const ReactVideo = (props)=>{
    // let params = new URLSearchParams(props.location.state);
    return(
        // <div>{params.get('name')}----{params.get('age')}</div>
        <div>{props.name} ---- {props.age}</div>
    )
}

export default ReactVideo;